# UniConnect Frontend

Next.js app for UniConnect.

From this folder:

```bash
npm install
npm run dev
```

The app runs at [http://localhost:3000](http://localhost:3000).

Copy `.env.example` to `.env.local` before starting.
